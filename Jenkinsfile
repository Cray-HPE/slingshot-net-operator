@Library('dst-shared@master') _

dockerBuildPipeline {
  repository = "slingshot"
  imagePrefix = "slingshot"
  app = "network-operator"
  name = "Slingshot Network Operator for CSM"
  description = "Slingshot Network Operator for CSM"
  product = "slingshot"
  dockerfile = "Dockerfile"
  masterBranch = "main"
  versionScript = "version.sh"
}